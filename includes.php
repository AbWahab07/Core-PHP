<?php

    // You can use the __DIR__ constant to refer to the directory that the current file is in.
    
    // Include

    /* Require
     * If you try to include a file that doesn't exist, then PHP will show a warning message, and continue executing.
     * If this is a problem in our applications,
     * we can use the require statement instead of the include to have PHP throw a fatal error when the file cannot be loaded.
    */

    /* Require Once
     * the require_once statement will only include a file once.
     * The second time the statement is executed, the file is ignored.
    */
 ?>
